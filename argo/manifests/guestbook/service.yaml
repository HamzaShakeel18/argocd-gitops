apiVersion: v1
kind: Service
metadata:
  name: guestbook-svc
  namespace: practice
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  selector:
    app: guestbook
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
